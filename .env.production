# NEXT_PUBLIC_ vars are safe to commit — they are exposed to the browser anyway.
# Committing here ensures the value is baked into every Docker build without
# requiring a GitHub Actions secret to be set.
NEXT_PUBLIC_STRAPI_URL=https://strapi.ekenhome.se
